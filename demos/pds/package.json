{
	"name": "@demo/pds",
	"version": "1.0.0",
	"description": "Demo PDS deployment",
	"type": "module",
	"private": true,
	"dependencies": {
		"@getcirrus/pds": "file:../.."
	},
	"devDependencies": {
		"@cloudflare/vite-plugin": "^1.17.0",
		"vite": "^6.4.1",
		"wrangler": "^4.93.0"
	},
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"deploy": "npm run build --prefix ../.. && vite build && wrangler deploy",
		"preview": "vite preview",
		"pds": "pds",
		"wrangler": "wrangler",
		"types": "wrangler types"
	},
	"keywords": [],
	"author": "Matt Kane",
	"license": "MIT"
}
